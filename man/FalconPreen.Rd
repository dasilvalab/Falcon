% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/FalconNest.R
\name{FalconPreen}
\alias{FalconPreen}
\title{Normalization of count data}
\usage{
FalconPreen(object, group, ...)
}
\arguments{
\item{object}{An instance of the \code{FalconNest} class.}

\item{group}{A character string specifying the group variable for normalization.}

\item{mean.pick}{An optional numeric value for selecting the mean threshold. If NULL (default),
mean.pick will be automatically calculated based on normalized count distribution.}

\item{cv.pick}{A numeric value specifying the coefficient of variation threshold (default is 20).}
}
\value{
An updated \code{FalconNest} object with normalized counts.
}
\description{
Normalizes the counts data in the \code{FalconNest} object based on specified parameters.
The normalization is applied according to the provided group variable and other thresholds.
}
\examples{
# Create example data
colData <- data.frame(sample = factor(rep(c("A", "B"), each = 5)))
counts <- matrix(data = abs(rpois(50 * 10, lambda = 5)), nrow = 50, ncol = 10)
colnames(counts) <- rownames(colData)
rownames(counts) <- 1:50
falcon_obj <- FalconNest(counts = counts, colData = colData, design = ~sample)
# Normalize counts
normalizedObject <- FalconPreen(object = falcon_obj, group = "sample", mean.pick = 1, cv.pick = 100)
}
